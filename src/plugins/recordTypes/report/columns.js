import { defineMessages } from 'react-intl';

export default {
  default: [
    {
      name: 'name',
      messages: defineMessages({
        label: {
          id: 'column.report.default.name',
          defaultMessage: 'Name',
        },
      }),
      width: 400,
    },
  ],
};
